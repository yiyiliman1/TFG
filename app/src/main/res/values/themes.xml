<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Base.Theme.JOIN" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:statusBarColor">#147001</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>

    <style name="Theme.JOIN" parent="Base.Theme.JOIN" />
</resources>
